{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstadoUsuario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idestadousuario</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estadousuario_show', { 'id': entity.idEstadoUsuario }) }}">{{ entity.idEstadoUsuario }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estadousuario_show', { 'id': entity.idEstadoUsuario }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('estadousuario_edit', { 'id': entity.idEstadoUsuario }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estadousuario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
