{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstadoUsuario</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idestadousuario</th>
                <td>{{ entity.idEstadoUsuario }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('estadousuario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('estadousuario_edit', { 'id': entity.idEstadoUsuario }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
