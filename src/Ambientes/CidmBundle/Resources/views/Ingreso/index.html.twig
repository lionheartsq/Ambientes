{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ingreso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idingreso</th>
                <th>Tag</th>
                <th>Fechacompleta</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ingreso_show', { 'id': entity.idIngreso }) }}">{{ entity.idIngreso }}</a></td>
                <td>{{ entity.tag }}</td>
                <td>{% if entity.fechaCompleta %}{{ entity.fechaCompleta|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ingreso_show', { 'id': entity.idIngreso }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ingreso_edit', { 'id': entity.idIngreso }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ingreso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
