{% extends '::base.html.twig' %}

{% block body -%}
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><h1>Estados listado</h1></div>
  <!-- Table -->
        <table class="table">
        <thead>
            <tr>
                <th>Idestado</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estado_show', { 'id': entity.idEstado }) }}">{{ entity.idEstado }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estado_show', { 'id': entity.idEstado }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('estado_edit', { 'id': entity.idEstado }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estado_new') }}">
                Crear un nuevo estado
            </a>
        </li>
                <li>
            <a href="{{ path('crud') }}">
                Regresar al inicio
            </a>
        </li>
    </ul>
  </div>
    {% endblock %}
