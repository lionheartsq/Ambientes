{% extends '::base.html.twig' %}

{% block body -%}
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><h1>Tipo de Elemento lista</h1></div>

    <table class="table">
        <thead>
            <tr>
                <th>Idtipoelemento</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipoelemento_show', { 'id': entity.idTipoElemento }) }}">{{ entity.idTipoElemento }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipoelemento_show', { 'id': entity.idTipoElemento }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipoelemento_edit', { 'id': entity.idTipoElemento }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipoelemento_new') }}">
                Crear nuevo tipo de elemento
            </a>
        </li>
                <li>
            <a href="{{ path('crud') }}">
                Regresar al inicio
            </a>
        </li>
    </ul>
  </div>
    {% endblock %}
