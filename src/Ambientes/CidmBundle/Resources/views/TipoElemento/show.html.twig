{% extends '::base.html.twig' %}

{% block body -%}
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><h1>Tipo de Elemento</h1></div>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idtipoelemento</th>
                <td>{{ entity.idTipoElemento }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoelemento') }}">
            Regresar a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('tipoelemento_edit', { 'id': entity.idTipoElemento }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
  </div>
{% endblock %}
