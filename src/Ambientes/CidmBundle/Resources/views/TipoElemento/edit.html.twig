{% extends '::base.html.twig' %}

{% block body -%}
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><h1>Tipo de Elemento editar</h1></div>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoelemento') }}">
            Regresar a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
  </div>
{% endblock %}
