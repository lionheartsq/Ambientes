{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jornada list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idjornada</th>
                <th>Horainicio</th>
                <th>Horafin</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('jornada_show', { 'id': entity.idJornada }) }}">{{ entity.idJornada }}</a></td>
                <td>{{ entity.horaInicio }}</td>
                <td>{{ entity.horaFin }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('jornada_show', { 'id': entity.idJornada }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('jornada_edit', { 'id': entity.idJornada }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('jornada_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
