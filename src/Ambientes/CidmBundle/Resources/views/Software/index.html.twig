{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Software list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idsoftware</th>
                <th>Categoria</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('software_show', { 'id': entity.idSoftware }) }}">{{ entity.idSoftware }}</a></td>
                <td>{{ entity.categoria }}</td>
                <td>{{ entity.nombre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('software_show', { 'id': entity.idSoftware }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('software_edit', { 'id': entity.idSoftware }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('software_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
