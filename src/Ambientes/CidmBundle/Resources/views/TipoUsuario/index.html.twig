{% extends '::base.html.twig' %}

{% block body -%}
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><h1>Tipo de Usuario lista</h1></div>

    <table class="table">
        <thead>
            <tr>
                <th>Idtipousuario</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipousuario_show', { 'id': entity.idTipoUsuario }) }}">{{ entity.idTipoUsuario }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipousuario_show', { 'id': entity.idTipoUsuario }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipousuario_edit', { 'id': entity.idTipoUsuario }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipousuario_new') }}">
                Crear nuevo tipo de usuario
            </a>
        </li>
                <li>
            <a href="{{ path('crud') }}">
                Regresar al inicio
            </a>
        </li>
    </ul>
  </div>
    {% endblock %}
