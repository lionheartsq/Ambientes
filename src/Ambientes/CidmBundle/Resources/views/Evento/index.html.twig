{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idevento</th>
                <th>Descripcion</th>
                <th>Fechainicio</th>
                <th>Fechafin</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('evento_show', { 'id': entity.idEvento }) }}">{{ entity.idEvento }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{% if entity.fechaInicio %}{{ entity.fechaInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaFin %}{{ entity.fechaFin|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('evento_show', { 'id': entity.idEvento }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('evento_edit', { 'id': entity.idEvento }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('evento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
