{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AccesoAmbientes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idacceso</th>
                <th>Fechacompleta</th>
                <th>Observaciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('accesoambientes_show', { 'id': entity.idAcceso }) }}">{{ entity.idAcceso }}</a></td>
                <td>{% if entity.fechaCompleta %}{{ entity.fechaCompleta|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.observaciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('accesoambientes_show', { 'id': entity.idAcceso }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('accesoambientes_edit', { 'id': entity.idAcceso }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('accesoambientes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
