{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstadoAccesoAmbientes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idestado</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estadoaccesoambientes_show', { 'id': entity.idEstado }) }}">{{ entity.idEstado }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estadoaccesoambientes_show', { 'id': entity.idEstado }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('estadoaccesoambientes_edit', { 'id': entity.idEstado }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estadoaccesoambientes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
